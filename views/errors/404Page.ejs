<% layout('layouts/boilerplate')%>
    <style>
        .loop-slider {
            .inner {
                display: flex;
                width: fit-content;
                animation-name: loop;
                animation-timing-function: linear;
                animation-iteration-count: infinite;
                animation-direction: var(--direction);
                animation-duration: var(--duration);
            }
        }

        .tag {
            display: flex;
            align-items: center;
            gap: 0 0.2rem;
            color: #e2e8f0;
            font-size: 0.9rem;
            border-radius: 0.4rem;
            padding: 0.7rem 1rem;
            margin-right: 1rem;

            span {
                font-size: 1.2rem;
                color: #64748b;
            }
        }

        .icon-image {
            height: 24px;
            margin: 0 10px;
        }

        @keyframes loop {
            0% {
                transform: translateX(0);
            }

            100% {
                transform: translateX(-50%);
            }
        }
    </style>

    <section class="container-one" style="height: 500px;">
        <h1 style="font-size: 5rem;">
            404
            <small style="font-size: 2rem;"> - Page Not Found</small>
        </h1>

        <div class="loop-slider" style="--duration:40000ms; --direction:normal;">
            <div class="inner">
                <% for(let i=0; i < 20; i++) {%>
                    <div class="tag">
                        <img class="icon-image" src="/data/images/fish-0.png" alt="Logo">
                    </div>
                    <%}%>
            </div>
        </div>
        <p>Oops! It looks like you are swimming in the wrong direction.</p>
        <p>Why not swim back to the <a href="/">home page</a>?</p>

    </section>